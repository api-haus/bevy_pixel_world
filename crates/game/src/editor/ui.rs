use bevy::prelude::*;

/// Configure editor UI system sets.
/// Keyboard shortcuts (F5/Escape) and yoleck's built-in Playtest button
/// handle play/edit toggling, so no custom UI needed here.
pub fn configure_system_sets(_app: &mut App) {
  // Reserved for future editor UI elements
}
